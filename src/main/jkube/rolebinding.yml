metadata:
  name: spring-cloud-k8s
subjects:
  - kind: ServiceAccount
    name: ${project.artifactId}
    namespace: default
roleRef:
  kind: Role
  name: spring-cloud-k8s
  apiGroup: rbac.authorization.k8s.io